# Copy to local.env in this directory (gitignored).
# Exact NAME=0 or NAME=1 lines only; no export, quotes, spaces, or shell code.
# Explicit environment values win, including 0. Unset flags default to off.
DOTFILES_MACOS_DESKTOP=0
DOTFILES_GHOSTTY_PANE_DIMMING=0
DOTFILES_PI_OVERWATCH=0
